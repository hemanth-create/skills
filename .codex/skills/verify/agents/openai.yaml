interface:
  display_name: "Verify"
  short_description: "Review a named target skeptically."
  default_prompt: "Use $verify to review this target and report findings first."

policy:
  allow_implicit_invocation: false
